---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - helmrelease.yaml

configMapGenerator:
  - name: loki-values
    files:
      - values.yaml
    namespace: flux-system
generatorOptions:
  disableNameSuffixHash: true
