---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: dimp-staging-auth-migrate
  namespace: flux-system
spec:
  interval: 1m
  imageRepositoryRef:
    name: dimp-auth-migrate
  filterTags:
    pattern: "^latest$"
  policy:
    alphabetical:
      order: asc
  digestReflectionPolicy: Always
