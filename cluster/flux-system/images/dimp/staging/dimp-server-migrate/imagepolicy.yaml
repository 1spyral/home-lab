---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: dimp-staging-server-migrate
  namespace: flux-system
spec:
  interval: 1m
  imageRepositoryRef:
    name: dimp-staging-server-migrate
  filterTags:
    pattern: "^latest$"
  policy:
    alphabetical:
      order: asc
  digestReflectionPolicy: Always
