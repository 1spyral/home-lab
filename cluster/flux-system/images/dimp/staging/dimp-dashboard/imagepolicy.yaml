---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: dimp-staging-dashboard
  namespace: flux-system
spec:
  interval: 1m
  imageRepositoryRef:
    name: dimp-staging-dashboard
  filterTags:
    pattern: "^latest$"
  policy:
    alphabetical:
      order: asc
  digestReflectionPolicy: Always
